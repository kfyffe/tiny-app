<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>
  <ul>
    <% for (let key in urls) {%>
      <li><%= key %> - <%= urls[key] %>
        <%= console.log(135, key, urls[key]) %>
        <form method="POST" action=<%=`/urls/${key}/delete`%>>
          <button type="submit">DELETE</button>
        </form>
      </li>
    <% }; %>
  </ul>
  <a href='#'>shorten</a>
</body>
</html>